import 'package:flutter/material.dart';

class StatsInterface extends StatelessWidget {
  const StatsInterface({super.key});

  @override
  Widget build(BuildContext context) {
    return  Center(child: Text("stats"),);
  }
}
